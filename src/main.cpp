#include <iostream>

int main() {
    std::cout << "FitSolver is working!" << std::endl;
    //the main boxes and items should be created here; the solver will do its job -
    // check whether the item fits inside the box
    return 0;
}